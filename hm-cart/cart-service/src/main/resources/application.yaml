server:
  port: 9003
spring:
  application:
    name: cart-service
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.41.128:8848
      config:
        server-addr: 192.168.41.128:8848
        file-extension: yaml
    openfeign:
      okhttp:
        enabled: true
  profiles:
    active: local

  config:
    import:
      - optional:nacos:jdbc.yaml
      - optional:nacos:log.yaml
      - optional:nacos:cart-service.yaml